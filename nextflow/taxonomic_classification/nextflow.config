process{
	executor= 'slurm'
	withName: collect_results {executor='local'}
	withName: barplot {executor='local'}
  queue = { task.memory > 100.GB ? 'nih_s10' : 'batch' }
	clusterOptions= '--account asbhatt'
}
